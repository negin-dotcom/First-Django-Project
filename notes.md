```bash
OrderListCreateView
    GET  → public
    POST → authenticated

OrderDetailView
    GET    → authenticated
    PATCH  → authenticated
    DELETE → authenticated

OrderItemListView
    GET → authenticated

OrderItemDetailView
    GET → authenticated
```